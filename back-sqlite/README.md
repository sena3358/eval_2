# back-sqlite

Spring Boot backend with SQLite and a modular, OOP structure inspired by the newApp layout.

## Run

1. `mvn spring-boot:run`
2. Test: `GET http://localhost:8080/api/sqlite/test`

The SQLite file will be created at `back-sqlite/data/app.db`.
